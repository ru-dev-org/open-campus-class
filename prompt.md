# 敵キャラクターAI 自動生成プロンプト

@Agentへ：
以下の【生徒の要件】を読み取り、`sketch.js` の仕様（マップデータや引数）を解析した上で、新しい敵キャラクターのアルゴリズムを実装してください。
またチャットのやり取りはすべて日本語でお願いします。

### 【実装ルール】
1. `students/ai_[ローマ字の名前].js` という新しいファイルを作成してください。
2. 作成したファイル内で、`GhostRegistry["名前"]` にオブジェクトを追加し、`logic` 関数を実装してください。
3. `logic` 関数は必ず `availableDirs` 配列に含まれる文字列 ('UP', 'DOWN', 'LEFT', 'RIGHT') のいずれかを `return` してください。
4. ファイル作成後、 `student_list.js` に作成したファイル名を追記し、他のファイル名はコメントアウトしてください。
5. 最後に、新しい敵が追加されたことを伝えて、ゲームをリロードするように促してください。

---
### 【生徒の要件】

▶︎ わたしの名前（表示名）：
（ここに名前を入力。例：田中）

▶︎ ローマ字の名前（ファイル名用）：
（ここにローマ字を入力。例：tanaka）

▶︎ ゴーストの色（英語で）：
（ここに色を入力。例：cyan, pink, orange）

▶︎ どんな動きにしたいか：
（ここに日本語で自由に指示を書いてください。例：基本はランダムに動くけど、プレイヤーが近くに来たら逃げる）